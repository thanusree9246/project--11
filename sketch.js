seaima = lodeimage("sea.png")
shipima1 = loadAnimation(//list the name of png files that you want to load);



function preload(){

}

function setup(){
  createCanvas(400,400);
  spriteName.addImage (seaima);
}

function draw() {
  background("blue");
 if (sea.x < 0){
sea.x = sea.width/2;


 }
}